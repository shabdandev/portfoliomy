import scss from "./Work.module.scss";

const Work = () => {
  return (
    <section id="work" className={scss.Work}>
      <div className="container">
        <div className={scss.content}>Work</div>
      </div>
    </section>
  );
};

export default Work;
